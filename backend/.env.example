# E-Commerce Platform Backend Environment Variables
# --------------------------------------------------
# This file serves as an example.
# To use it, copy this file to a new file named .env in the same directory (`backend/`)
# and replace the placeholder values with your actual configuration.
#
# IMPORTANT: DO NOT commit the .env file to your version control system (e.g., Git).
# Add `.env` to your `.gitignore` file.

# --- Server Configuration ---
# The environment mode your application is running in (e.g., development, production)
NODE_ENV=development

# The port your Node.js/Express server will listen on
PORT=5000

# --- Database Configuration ---
# Your MongoDB connection string.
# Replace <user>, <password>, <cluster-url>, and <database-name> with your actual credentials.
# Example: mongodb+srv://myuser:mypassword@mycluster.mongodb.net/ecommerceDB?retryWrites=true&w=majority
MONGO_URI=your_mongodb_connection_string

# --- JSON Web Token (JWT) Authentication ---
# A long, complex, and random secret string for signing tokens.
# Use a strong, unpredictable string. You can generate one using:
# `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
JWT_SECRET=your_super_secret_jwt_key_that_is_at_least_32_characters_long

# The expiration time for JWTs. Examples: "1h" (1 hour), "7d" (7 days), "30m" (30 minutes)
JWT_EXPIRES_IN=1d

# --- Stripe API Keys ---
# Get your API keys from the Stripe dashboard: https://dashboard.stripe.com/apikeys
#
# Your Stripe publishable key (safe to expose on the client-side).
STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_STRIPE_PUBLISHABLE_KEY

# Your Stripe secret key (MUST be kept secret and secure on the server).
STRIPE_SECRET_KEY=sk_test_YOUR_STRIPE_SECRET_KEY

# --- CORS Configuration ---
# The URL of your frontend application to allow Cross-Origin Resource Sharing.
# For local development with a standard React setup, this is typically http://localhost:3000.
# For production, this should be the domain of your deployed frontend.
FRONTEND_URL=http://localhost:3000